import React, { Component } from "react";
import { render } from "react-dom";
// import "./Timer.css";


const Timer = props => (
  <div>
    {/* Timer placeholder */}
  </div>
);

export default Timer;